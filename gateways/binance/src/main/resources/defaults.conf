app: {
    name: "binance"
    source: "binance"
}

kafka: {
    topic: "md.binance"
    hb.key: "binance"
    bootstrap.servers: "localhost:9092"
    client.id: "BinanceProducer"
    throttle.second: 2
    application.id: "BinanceThrottler"
}