app: {
    name: "stream-transformer"
}

kafka: {
    topics: ["md.gdax", "md.bitstamp", "md.bitfinex", "md.anxpro"]
    bootstrap.servers: "localhost:9092"
    application.id: "StreamTransformer"
}

symbols: ["btcusd"]